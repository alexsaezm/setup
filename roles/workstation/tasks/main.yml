---
#- name: 'Ensure all packages are installed'
#  include_tasks: 'packages_{{ ansible_os_family | lower }}.yml'

- name: 'Ensure all tools are configured'
  include_tasks: 'configure_{{ item }}.yml'
  with_items:
    - 'vim'
    - 'tmux'
    - 'zsh'
